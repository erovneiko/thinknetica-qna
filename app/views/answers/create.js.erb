$('.answer-errors').html(`<%= render 'shared/errors', resource: @answer %>`);

<% if @answer.persisted? %>
  $('.new-answer #answer_files').val('');
  $('.new-answer #answer_body').val('');
  $('.new-answer .nested-fields').not(':first').remove();
  $('.new-answer .nested-fields input').val('');
<% end %>
